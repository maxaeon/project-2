{{> navbar}}
<main class="flex flex-column justify-around lg:flex-row flex-wrap p-0">
    <div class="w-full text-center text-3xl my-3"> Welcome, {{username}}
    </div>
    {{>my-posts}}
    {{>dashboard-main}}

</main>